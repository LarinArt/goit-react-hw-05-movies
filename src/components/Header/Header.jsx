import { Navigation } from 'components/Navigation/Navigation';
import { MovieHeader } from './Header.style';

export const Header = () => {
  return (
    <MovieHeader>
      <Navigation />
    </MovieHeader>
  );
};
