import { Navigation } from 'layout/common/Navigation/Navigation';
import { MovieHeader } from './Header.style';

export const Header = () => {
  return (
    <MovieHeader>
      <Navigation />
    </MovieHeader>
  );
};
